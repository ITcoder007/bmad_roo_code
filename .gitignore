# === IDEs ===
.idea/
*.iml
*.iws
.vscode/
# Optional (Eclipse)
.project
.classpath
.settings/

# === Build outputs ===
**/target/
out/

# === JVM crash dumps / memory ===
hs_err_pid*
replay_pid*
*.hprof

# === Logs & PIDs ===
*.log
*.out
*.pid

# === OS files ===
**/.DS_Store
Thumbs.db

# === Environment / Secrets ===
#.env
#.env.*
!.env.example
!.env.sample
# Spring Boot local overrides (keep examples under version control if needed)
application-local.properties
application-local.yml
application-local.yaml
# If and only if you actually use per-developer local props:
# *.properties.local

# === Maven Wrapper (KEEP IN VCS) ===
# Do NOT ignore the wrapper — it should be committed for reproducible builds.
!.mvn/wrapper/maven-wrapper.jar
!.mvn/wrapper/maven-wrapper.properties
!.mvn/jvm.config
!.mvn/maven.config
# This file is generated during builds and can be ignored:
.mvn/timing.properties

# === Dependencies / Caches ===
# Node
**/node_modules/

# === Packaged artifacts (generated; don't commit) ===
*.class
*.jar
*.war
*.ear

# === Editor temp files ===
*.swp
*.swo
*.tmp
*.temp

# === Frontend build/test (optional) ===
**/dist/
**/build/
**/.vite/
**/.parcel-cache/
**/.cache/
**/.turbo/
**/.eslintcache
**/.stylelintcache
**/coverage/
**/cypress/screenshots/
**/cypress/videos/
**/cypress/downloads/
**/playwright-report/
**/test-results/
**/blob-report/

# === Spring Boot Devtools (local only) ===
spring-boot-devtools.properties
spring-boot-devtools.yml
spring-boot-devtools.yaml

# === Maven misc ===
dependency-reduced-pom.xml

# === Lockfiles (commit these; ensure they are NOT ignored) ===
!yarn.lock
!pnpm-lock.yaml

/package-lock.json
/frontend/package-lock.json
